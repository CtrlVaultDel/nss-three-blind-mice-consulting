// Imports
import { employeeList } from "./employees/employeeList.js";
import { customerList } from "./customers/customerList.js";

// Display the list of employees
employeeList();

// Prepare customers to be displayed (will be hidden by default)
customerList();